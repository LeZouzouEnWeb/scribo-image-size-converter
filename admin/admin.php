<?php

// Utiliser la classe
// $scribo_init = new ScriboInit(ISC_PLUGIN_FILE);
// debug :
// error_log("Start Admin");

// Initialiser la classe avec les valeurs appropriées
$isc_settings = new ISC_Settings(ISC_SCRIBO_INIT);